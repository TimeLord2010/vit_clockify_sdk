name: vit_clockify_sdk
description: A Dart SDK for the Clockify API, providing a clean and type-safe interface for time tracking operations.
version: 1.2.0
repository: https://github.com/TimeLord2010/vit_clockify_sdk

environment:
  sdk: ^3.10.7

dependencies:
  dio: ^5.4.0
  iso8601_duration: ^1.2.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
