name: vit_clockify_sdk
description: A Dart SDK for the Clockify API, providing a clean and type-safe interface for time tracking operations.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.7

# Add regular dependencies here.
dependencies:
  dio: ^5.4.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
